DiscordRPC = {
    appId = GetConvar("appId", ""), -- Your application's ID
    bigAsset = GetConvar("bigAsset", ""), -- Your server logo
    bigAssetText = GetConvar("bigAssetText", "Join the server!"), -- Text on big asset hover
    discordAction = GetConvar("discordAction", ""), -- Invite link to your server
    websiteAction = GetConvar("websiteAction", ""), -- Link to your website

    refreshTime = GetConvar("refreshTime", "15"), -- Time between updates to your Rich Presence in seconds
    speedUnit = GetConvar("speedUnit", "kmh") -- Set it kmh or mph
}